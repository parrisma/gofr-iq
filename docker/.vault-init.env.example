# Example Vault initialization credentials (do NOT use in production)
# Copy to docker/.vault-init.env with real values after vault init.
export VAULT_UNSEAL_KEY="replace-with-unseal-key"
export VAULT_TOKEN="replace-with-root-token"
