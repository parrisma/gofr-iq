# Example docker environment (non-secret placeholders)
# Copy to docker/.env and replace with real values; keep secrets out of git.
GOFR_JWT_SECRET=replace-with-jwt-secret
VAULT_ROOT_TOKEN=replace-with-vault-root-token
# Optional: OpenRouter API key for LLM generation
GOFR_IQ_OPENROUTER_API_KEY=replace-with-openrouter-key
